import {sum} from '../src/sum.js';

test("test sum 1 , sum(1,2) must be 3",()=>{
    const result = sum(1,2);
    expect(result).toBe(3);
})

test("test sum 2, sum(1,2) must be 3",()=>{
    const result = sum(1,2);
    expect(result).toBe(3);
})

test("test sum 3, sum(1,2) must be 3",()=>{
    const result = sum(1,2);
    expect(result).toBe(3);
})